[project]
name = "avt-analyzer"
version = "0.1.0"
description = "Python analyzer for AVT Execution Flow Graphs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
avt = "avt_analyzer.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
