# AVT Viewer

Static/local web viewer for AVT Execution Flow Graph JSON.

Planned stack: Vite + React + TypeScript. The visualization library will be chosen after the spike in `spikes/visualization-libraries`.
